<div class="mt-6 overflow-x-auto bg-white p-2 sm:p-6 rounded-lg shadow-lg">
    <h2 class="text-xl md:text-2xl font-bold px-2 sm:px-6 py-4">
        {{ title }}
    </h2>
    <table class="min-w-full divide-y divide-gray-200 table-fixed">
        {% include "templates/taskTableHeaders.peb" %}
            {% for note in notes %}
                {% include "templates/taskTableContent.peb" %}
            {% endfor %}
    </table>
</div>
